<p>Successfully connected to server!</p><h1>
    Devouring Burgers</h1>

<h2>Burgers that you haven't eaten yet.</h2>

 <ul>
    {{#each burgers}} 
    {{#unless devoured}} 

<li>
    {{burger_name}}
    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devoured}}That was good!
        {{else}}Eat your burger!{{/if}}
    </button>
</li>


    {{!-- {{> burgers/burger-block devoured=true}}  --}}
    {{/unless}} 
    {{/each}}
</ul>

<h2>Burgers you have eaten.</h2>

<ul>
    {{#each burgers}}
     {{#if devoured}} 
<li>
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devoured}}That was good!{{else}}Eat your burger!{{/if}}
    </button>
</li>



     {{!-- {{> burgers/burger-block devoured=false}}  --}}
     {{/if}} 
     {{/each}}
</ul>

<h2>Add a burger.</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="burger_name"> //change made above from name to burger_name
    </div>

    <div class="form-group">
        <label for="slee">Devoured?</label>
        <br>
        <input type="radio" name="devoured" value="1" checked> Devoured!
        <br>
        <input type="radio" name="devoured" value="0"> To be later savoured...
    </div>

    <button type="submit">Add burger</button>
</form>